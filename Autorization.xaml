<Page x:Class="StudentGroup.Autorization"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:StudentGroup"
      mc:Ignorable="d" 
      d:DesignHeight="390" d:DesignWidth="800"
      Background="{StaticResource LightBackgroundBrush}"
      Title="Авторизация">

    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Логин:" HorizontalAlignment="Right" Margin="0,0,10,5" Style="{StaticResource PrimaryTextBlockStyle}"/>
            <TextBox Grid.Row="2" Grid.Column="1" x:Name="login" Width="200" Margin="0,0,0,5" Style="{StaticResource PrimaryTextBoxStyle}"/>
            <TextBlock Grid.Row="3" Grid.Column="0" Text="Пароль:" HorizontalAlignment="Right" Margin="0,0,10,5" Style="{StaticResource PrimaryTextBlockStyle}"/>
            <PasswordBox Grid.Row="3" Grid.Column="1" x:Name="password" Width="200" Margin="0,0,0,5" Style="{StaticResource PrimaryPasswordBoxStyle}"/>
            <Button Grid.Row="4" Grid.Column="1" Content="Войти" Style="{StaticResource PrimaryButtonStyle}" Click="Button_Click"/>
        </Grid>
    </Grid>
</Page>
